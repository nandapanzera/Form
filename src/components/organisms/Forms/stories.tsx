import { Meta } from '@storybook/react'
import { Form } from '.'

const meta: Meta = {
  title: 'Organisms/Form',
  component: Form,
  tags: ['autodocs'],
}

export default meta

export const Template = () => <Form />
